#include <stdio.h>
#include <string.h>

#define STRLEN 10

void sostituiscix(char svecchia[], char sresto[]){
	char risultato[STRLEN];
	strrncpy()
}

void genera(char str[]){
	int i;
	
	//scorro la stringa finché trovo la prima x
	for(i = 0; str[i] != '\0' && str[i] != 'x'; i++) {
		sostituiscix()
	}	
}

int main() {
	//dichiarazione variabili
	char stringa[STRLEN];
	
	//input stringa
	printf("Inserisci una stringa di 0, 1 e x -->\t");
	scanf("%s", stringa);
	
	printf("%s\n", stringa);
	
	genera(stringa);
	
	return 0;
}
